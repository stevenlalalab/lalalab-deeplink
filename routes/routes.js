const app = require('express')
const router = app.Router()

// router.get('/', (req, res) => {
//   console.log(req.device)
//   res.status(200).send(`${is.firefox()}`)
// })

module.exports = {
  router
}